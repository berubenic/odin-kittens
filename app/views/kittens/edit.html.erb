<h1>Edit Kitten</h1>

<% if flash[:error] %>
   <p><%= flash[:error] %></p>
<% end %>

<%= render 'form', kitten: @kitten %>

<%= link_to "Destroy", kitten_path(@kitten), method: :delete, data: { confirm: "Are you sure?" } %>